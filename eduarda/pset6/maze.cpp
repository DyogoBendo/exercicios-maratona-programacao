#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m, k;
    cin >> n >> m >> k;
    vector<vector<bool>> insert(vector<bool> var()); 
    vector<vector<char>> matrix;

    for(int i = 0; n < n; i++){
        for(int j = 0; j < m; j++){
            char a;
            cin >> a;
            matrix[i][j] = a;
        }
    }

    //se tem um # embaixo ou em cima do matrix[i][j], adiciono um X

    for(int i = 0; i < n; i++){
        for(int j = 0; j < n; j++){
            
        }
    }


}